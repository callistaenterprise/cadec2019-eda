server:
  port: 8081
logging:
  pattern:
    console: "%date{HH:mm:ss.SSS} %msg%n"
  level:
    root: WARN
    se:
      callista:
        cadec:
          eda: INFO
spring:
  main:
    banner-mode: "OFF"  
integration:
  customer: http://${DOCKER_HOST_IP:localhost}:8083/customer
